#include <iostream>

using namespace std;
int count_test = 0;
void test(int num, int max) {
	if (num >= max) {
		statements
	}
}
int main(int argc, char *argv[]) {
	int num, max;
	cin >> num;
	cin >> max;
	if (max >= num) {
		test(num, max);
	} else {
		cout << "Please Enter true";
	}
	
	cout << count_test;
}